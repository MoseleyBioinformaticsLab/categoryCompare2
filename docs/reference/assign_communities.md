# assign communities

given a `cc_graph`, find communities of nodes based on their
connectivity and weights.

## Usage

``` r
assign_communities(in_graph)
```

## Arguments

- in_graph:

  the `cc_graph` object to use

## Value

list
