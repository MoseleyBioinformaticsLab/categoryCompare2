# vis in visNetwork

Visualize a `cc_graph` in `visNetwork`, with selection for communities
if that exists.

## Usage

``` r
vis_visnetwork(in_graph_info)
```

## Arguments

- in_graph_info:

  the graph structure from `graph_to_visnetwork`
