# gsea_features constructor

Creates a gsea_features-class object.

## Usage

``` r
gsea_features(ranks, annotation)
```

## Arguments

- ranks:

  a named vector of ranks

- annotation:

  the annotation object

## Value

gsea_features-class
