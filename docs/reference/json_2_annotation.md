# json to annotation

Given a JSON based annotation object, read it in and create the
\`annotation\` for actually doing enrichment.

## Usage

``` r
json_2_annotation(json_file)
```

## Arguments

- json_file:

  the json annotation file

## Value

annotation object
